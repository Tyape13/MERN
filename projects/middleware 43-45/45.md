

### 1 - Data Validation
browser or webserver needs to check if the data is correct or within constrains.
when adding new post if we put incomplete information, we don't want it to be accepted, incomplete info in required fields gives error in ugly format. 
but we want to show them please enter everything then only you can update or add button activation. stuff.

we put 'required' attribute in html itself.
and then she used bootstrap ones cause different browser ma shows different message to fill in the input field.

Let's Learn how Bootstrap works even though i m not going to implement.
WE can exactly see what's the code in bootstrap classes stuff. idk bro.
what she does is:
novalidate attribute in form element.
then class= "needs-validation" in form element.
then copy paste javascript code to new file in public/js folder. that's all then it works. 


### 2 - Success and Failure Text when field is filled correctly or not.
bootstrap logics. no need to learn. we skip.

one good thing she says is: if we use post request directly through hotscoch or someother tool like httpToolkit. 
then we can BYPASS this restrictions. 

so server side checking is necessary.


### 3 - Custom Error Handling
She talks about when input needs to be number but string pass handyo vane, Error hunxa. 
it will be async error since req.body ko data will be tried to save in database and error will pop up.

she just put try and catch block in main project. 

### 4 - custom asyncWrap in major project

### 5 - ExpressError in major project
if (!req.body.listing || Object.keys(req.body.listing).length === 0) {
        // If the listing data is missing or empty, throw an error with a 400 status code.
        throw new ExpressError(400, "Bad Request: No listing data provided.");
    }
yes yeai thyo main stuff.


### 6 - error.ejs
error needs to look good. who cares right now. 

### 7 - Schema Validation 
currently it only throws error when every field is not filled. 
but we can also put validation in schema itself.

she first uses first option as in if conditions for checking if listing data is provided or not.
so, 
we use package called 'joi' that validates Schema for SERVER SIDE.

learning new package. creating whole listingSchema.js with this joi package.
then validating in 1 line. 
it's easy. 

### 8 - Schema Validation as middleware Function
const validateListing = (req, res, next) => {
    const { error } = listingSchema.validate(req.body.listing);
    if (error) {
        const msg = error.details.map(el => el.message).join(',');
        throw new ExpressError(400, msg);
    } else {
        next();
    }
}
 
then pass this middleware function in post route.

