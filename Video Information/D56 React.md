Let's Learn React cause this can be learned individually/separaterly.
This is more Important cause here it's just IMPORTANT STUFF to Learn. than going all in document and being an IDIOT.


### 1 - What is React
JS Library for creating UserInterfaces(UI).
react.dev - website
we will just learn important ones. 

component - individual pieces or section , button, card. reusuable just like function.
almost everything on react is component. (function jasto for frontend section ui)
'like/love' button component, description component. making it whole as card component. it can be as big as whole page too. 

Components are rendered, functions are called/invoked.
Hierarchy Structure hunxa.
One component contains html, css and Js. whole.

codesandbox.io vanne website from react.dev website

JSX - javascript with html is .jsx

### 2 - JSX
javascript extension syntax
it let's us write html direcly inside Js.

babel tool transpiles JSX to Js then rendering is done of pure js.
JSX is easy to implement and more redeable.

babeljs.io ma we can paste JSX code and see it gets changed to js.


### 3 - setting up local env
to make react run we need some setup like vite.
Vite (quick and faster)
`npm create vite@latest`
`npm run dev` // to start server

